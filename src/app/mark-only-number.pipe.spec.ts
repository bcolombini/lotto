import { MarkOnlyNumberPipe } from './mark-only-number.pipe';

describe('MarkOnlyNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new MarkOnlyNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
